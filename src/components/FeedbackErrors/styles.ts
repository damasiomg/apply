import styled from 'styled-components';

export const Error = styled.span`
    color: #c53030;
    font-size: 15px;
    position: relative;
    left: 1px;
    margin-bottom: 10px;

`;

export const BadRequest = styled.section`
    display: flex;
    width: 100%;
    flex-direction: column;
`; 
